<div class="">
  <div class="col-12 col-lg-6">
    <h1>Citizen Reporting Solution App</h1>
    <p>
      We would be giving you latest News.STAY TUNED!!!
    </p>

    <div class="d-grid gap-2 d-md-flex justify-content-md-start">
      <a
        href="/explore-latest"
        class="btn btn-primary btn-dark btn-lg px-4 me-md-2"
        >Explore Latest</a
      >
      <a
        href="/explore-random"
        class="btn btn-outline-secondary btn-lg px-4 me-md-2"
        >Show Random</a
      >
    </div>
  </div>
</div>
<!-- 
<div class="row row-cols-2 row-cols-lg-6 g-2 g-lg-3 py-4">
  <% if(categories != ' ') { %> <% categories.forEach(function(category, index)
  { %>
  <a href="/categories/<%- category.name %>" class="col text-center category__link">
    <div class="category__img shadow">
      <img
        src="img/<%- category.image %>"
        alt="<%- category.name %>"
        loading="lazy"
      />
    </div>
    <div class="pt-1"><%- category.name %></div>
  </a>
  <% }) %> <% } %>

  <a href="/categories" class="col text-center category__link">
    <div class="category__img shadow">
      <img src="img/view-all.jpg" alt="View All" loading="lazy" />
    </div>
    <div class="pt-1">View All</div>
  </a>
</div> -->

<section class="pb-4 pt-4">
  <div class="d-flex mb-2 align-items-center">
    <h2>Citizen solution App</h2>
    <a href="/explore-latest" class="ms-auto">View more</a>
  </div>

  <div class="row row-cols-2 row-cols-lg-5">
    <% if(food != "") { %> <% food.latest.forEach(function(recipes,index) { %>
    <a
      href="/recipes/<%= recipes._id %>"
      class="col text-center category__link"
    >
      <div class="category__img category__img--large shadow">
        <img
          src="/uploads/<%= recipes.image %>"
          alt="<%= recipes.name %>"
          loading="lazy"
        />
      </div>
      <div class="pt-1"><%= recipes.name %>"</div>
    </a>
    <% }) %> <% } %>
  </div>
</section>


<!-- <section class="pb-4 pt-4">
  <div class="d-flex mb-2 align-items-center">
    <h2>American Recipes</h2>
    <a href="/categories/American" class="ms-auto">View more</a>
  </div>

  <div class="row row-cols-2 row-cols-lg-5">
    <% if(typeof food.american != '') { %> <%
    food.american.forEach(function(recipes,index) { %>
    <a
      href="/recipes/<%= recipes._id %>"
      class="col text-center category__link"
    >
      <div class="category__img category__img--large shadow">
        <img
          src="/uploads/<%= recipes.image %>"
          alt="<%= recipes.name %>"
          loading="lazy"
        />
      </div>
      <div class="pt-1"><%= recipes.name %>"</div>
    </a>
    <% }) %> <% } %>
  </div>
</section>

<section class="pb-4 pt-4">
  <div class="d-flex mb-2 align-items-center">
    <h2>Chinese Recipes</h2>
    <a href="/categories/American" class="ms-auto">View more</a>
  </div>

  <div class="row row-cols-2 row-cols-lg-5">
    <% if(typeof food.chinese != '') { %> <%
    food.chinese.forEach(function(recipes,index) { %>
    <a
      href="/recipes/<%= recipes._id %>"
      class="col text-center category__link"
    >
      <div class="category__img category__img--large shadow">
        <img
          src="/uploads/<%= recipes.image %>"
          alt="<%= recipes.name %>"
          loading="lazy"
        />
      </div>
      <div class="pt-1"><%= recipes.name %>"</div>
    </a>
    <% }) %> <% } else { %>
    <p>No items Found.</p>
    <% } %>
  </div>
</section> -->

<!-- Submit Section -->
<!-- <section class="flex px-4 py-5 my-5 text-center">
  <img
    src="/img/publish-recipe.png"
    class="d-block mx-auto mb-4 img-fluid"
    alt="Publish your recipes for Free"
    width="566"
    height="208"
    loading="lazy"
  />
  <h1 class="text display-5 fw-bold">Publish your recipes for Free</h1>
  <div class="col-lg-6 mx-auto">
    <p class="text lead mb-4">Publish your recipes for Thousand of People.</p>
    <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
      <a href="/submit-recipe" class="btnn btn-primary btn-white btn-lg"
        >Submit
      </a>
    </div>
  </div>
</section> -->
